// getters
const getters = {
  breadListState: (state) => {
    return JSON.parse(sessionStorage.getItem('breadListStorage')) || state.breadListState;
  },
  menuListState: (state) => {
    return state.menuListState;
  }
};

export default getters;
