// actions
// import console from '../../../../api/common';

const actions = {
  activeRoute ({ commit }, routers) {
    commit('activeRoute', routers);
  },
  collapse ({ commit }, router) {
    commit('collapse', router);
  }
};

export default actions;
